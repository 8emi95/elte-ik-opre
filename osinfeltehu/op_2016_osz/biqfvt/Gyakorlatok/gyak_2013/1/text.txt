First line
Second line
Last line
if (argc!=3){perror("You have to use program with two arguments, the file names copy_from copy_to\n");exit(1);}
 //perror writes out to  error channel - it can be the normal screen
 int f,g;
 
 f=open(argv[1],O_RDONLY); 
 //there is an access function with which we can see e.g 
 //whether the file is exist or not
 //access(filename,F_OK);
 //open 1. parameter= file name
 //open 2. parameter for what we want to use the file 
 //O_RDONLY=only for reading,O_WRONLY-only