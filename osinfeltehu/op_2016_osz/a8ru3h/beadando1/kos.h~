//#ifndef KOS_H
//#define KOS_H

#include <time.h>

struct rendezveny_t {
  int id;
  char nev[256];
};

struct vendeg_t {
  int rendezveny_id;
  char nev[256];
  char mail[256];
  time_t jelentkezes_ideje;
};

//#endif
