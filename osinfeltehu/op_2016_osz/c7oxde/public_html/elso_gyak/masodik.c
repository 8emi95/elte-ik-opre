#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main(){
 char* pointer;
 char* pointer2 = pointer;
 printf(pointer,pointer2);
  char* pointer;
   char* pointer2 = pointer;
    printf(pointer,pointer2);
    (){
     char* pointer;
      char* pointer2 = pointer;
       printf(pointer,pointer2);
         char* pointer;
            char* pointer2 =(){
             char* pointer;
}