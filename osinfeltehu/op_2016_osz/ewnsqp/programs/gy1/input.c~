#include <stdio.h>

int main(int argc, char** argv)
{
  // char scanFInput[30];
  // scanf("%s", scanFInput);
  
  char* fgetsInput[30];
  fgets(fgetsInput, 30, stdin);
  
  printf(scanFInput);
  printf(fgetsInput);
  
  return 0;
}