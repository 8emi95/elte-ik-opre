'''MONTE CARLO (25 pont)
Számoljuk ki az egység sugarú kör területét Monte Carlo módszerrel. Vegyünk n
db pontot és helyezzük el õket a (-1,-1) -- (1,1) négyzetbe véletlenül. Jó
pontok azok, amelyek a körön belül vannak (rosszak pedig kívül). A terület egy
közelítését kapjuk a 4*(jó/összes pont) számmmal. Viszgáljuk meg animáció
segítségével, hogy hogyan változik a közelítés pontossága n növelésével.
(Legyen ábra a pontokról, valamint a területrõl és a 
hibáról az n függvényében.
Készíts eljárást, amely a fenti módszerrel kiszámolja egy függvény határozott
integrálját a [-1,1] tartományban. +15 pont.'''




#Függvény

def monte_carlo(n, f):
    good = 0
    bad = 0
    for i in range(0, n):
        x = random_element(0,1)
        y = 1#rand(0..1)
        if y < f(x):
            good = good + 1
        else:
            bad = bad + 1
    return f(4)




#Tesztelés:

n = 10

def f_circle(x):
    return sqrt(1 - x^2)

def f1(x):
    return x^2

def f2(x):
    return x^3

def f3(x):
    return ln(x)

monte_carlo(n, f_circle)
monte_carlo(n, f1)
monte_carlo(n, f2)
monte_carlo(n, f3)