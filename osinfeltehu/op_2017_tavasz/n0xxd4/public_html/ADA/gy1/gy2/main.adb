with Ada.Text_IO, Ada.Integer_Text_IO, Fact, Swap, Lnko;
use Ada.Text_IO, Ada.Integer_Text_IO;

procedure Main is
--n : Natural;	(1)
--p1 : Integer;	(2)
--p2 : Integer;	(2)
--k : Positive; (3)
--l : Positive; (3)

	--function Lnko(k, l : Positive) return Positive is
	--begin
		--if (k mod l = 0) then return l;
		--else return Lnko(l, k mod l);
		--end if;
	--end Lnko;

begin
-- (1) Faktorialis
	--Put("Adja meg a szamot: ");
	--Get(n);
	--Put("A szam faktorialisa: ");
	--Put(Fact(n));
	
-- (2) Paraméterek megcserélése
	--Put_Line("Adja meg a ket parametert!");
	
	--Put("p1 := ");
	--Get(p1);
	
	--Put("p2 := ");
	--Get(p2);
	
	--Swap(p1, p2);
	
	--Put_Line("");
	--Put_Line("Csere utan: ");
	--Put_Line("p1 = " & Integer'Image(p1));
	--Put_Line("p2 = " & Integer'Image(p2));
	
-- (3) Legnagyobb közös osztó
	--Put_Line("Adjon meg 2 szamot!");
	
	--Put("k := ");
	--Get(k);
	
	--Put("l := ");
	--Get(l);
	
	--Put_Line("A legnagyobb kozos osztojuk: " & Integer'Image(Lnko(k, l)));
	
-- (4) Legnagyobb közös osztó beágyazása
-- (5) Rekurzív faktoriális
-- (6) Lnko Euklideszi algoritmussal
-- (7) Lnko rekurzívan
	
	null;
end Main;
